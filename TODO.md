# TODO

- [x] Add `-r/--run` option to CLI routing in `darkglitch.py`.

- [x] Add a build helper that generates a Windows listener `.exe` via PyInstaller.

- [x] Add a small listener entrypoint script for PyInstaller (imports and runs `listen_bash_mode`).

- [x] Add `pyinstaller` as a build dependency (update `requirements.txt`).

- [x] Update README with new `darkglitch -r` usage and output location.

- [ ] Smoke test CLI help/dispatch (at least for `-r` path) and ensure no regressions.

